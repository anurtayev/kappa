query Search($searchInput: SearchInput!, $pageSize: Int!, $nextToken: String) {
  search(
    searchInput: $searchInput
    pageSize: $pageSize
    nextToken: $nextToken
  ) {
    items {
      attributes {
        attribute {
          name
          type
        }
        value
      }
      id
      tags
    }
    nextToken
  }
}
