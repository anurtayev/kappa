type FolderConnection {
  scrollTop(locationKey: String!): Int!
}

type Query {
  slideId(index: Int!): String
  numberOfSlides: Int
  userProfile: UserProfile
}

type UserProfile {
  email: String!
}
